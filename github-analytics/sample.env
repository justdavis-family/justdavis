# Classic GitHub PAT with `repo` scope, used for:
#   - Local cassette recording: uv run pytest --record-mode=new_episodes
#   - Smoke testing: mise run :collect:smoke
# In production, this is stored as the ANALYTICS_DATA_PAT Actions secret.
GITHUB_TOKEN=""
